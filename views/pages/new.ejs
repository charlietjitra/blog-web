<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <title><%= index !== null ? 'Edit Post' : 'Create New Post'%></title>
</head>
<body>

<h1><%= index !== null ? 'Edit Post' : 'Create New Post'%></h1>
<form action="<%= index !== null ? '/edit/'+index : '/new/' %>" method="post">
    <label for="title">Title: </label>
    <input type="text" name="title" value="<%= post.title %>" id="title" required>
    <label for="content">Content: </label>
    <textarea type="text" name="content" id="content" required><%= post.content  %></textarea>
    <button type="submit"><%= index !== null ? 'Update' : 'Create'%></button>
</form>

</body>
</html>